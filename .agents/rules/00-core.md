# Workspace operating rule

Follow the repository root `AGENTS.md`.

Do not perform cold-start repository exploration.

Default to targeted retrieval. Expand scope only when a concrete unresolved
dependency requires it.

Shell output is already handled by the configured sqz hook.
